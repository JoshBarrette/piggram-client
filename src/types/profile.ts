export type PiggramProfile = {
  userId: string;
  firstName: string;
  lastName?: string;
  picture: string;
  postsCount: number;
  followingCount: string;
  followersCount: string;
  joinDate: string;
};
